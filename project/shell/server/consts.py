from typing import Dict

import sys
import os
# sys.path.append(os.getcwd() + '/project/shell/')

from datatype import *


# XSETS = Dict[bytes, bytes]
XSETS = dict()

# USETS = Dict[bytes, bytes]
USETS = dict()

# ASETS = Dict[bytes, Aset_item]
ASETS = dict()

